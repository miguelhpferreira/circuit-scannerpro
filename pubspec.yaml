name: circuit_scanner_pro
description: Circuit Scanner PRO
version: 1.0.0+1

environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter

  image_picker: ^1.0.7
  google_mlkit_text_recognition: ^0.13.0
  google_mlkit_barcode_scanning: ^0.12.0
  path_provider: ^2.1.2
  share_plus: ^7.2.1
  url_launcher: ^6.2.6

flutter:
  uses-material-design: true
